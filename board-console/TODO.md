P1: Add google login with call to private endpoint to access list of allowed user and then auth the app.
P2: Move the UI to react + SSE instead of api pooling
P3: Bring Design System and keep the console UI and UX consistent
